# Music

The soundtrack is not part of this repository. The game looks for
`<track>.mp3` here for each track named in `MusicTrack`
(`packages/client/src/data/clientConstants.ts`) and plays silence for any
that are missing, so it runs fine without them.
